Tasks:
	- Переписать код в компонентный подход;
	- Создать комп. "app-input" который будет принимать "label", "value", "valid" (может ещё что-то на выш выбор) и будет это всё выводить, а также сообщать родительскому комп. по "input" (не нативное название события, напр. "updated");
	- Анализ стартового значения строк ввода на основе "value";
	- Создать прогрессбар, показывающий сколько полей заполнено;
	- Блокировка кнопки "Отправить данные", пока все поля не станут валидными;
	- Показ итоговой таблицы с результатами, если все поля валидны.
